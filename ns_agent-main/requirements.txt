# Core API
fastapi>=0.104.0
pydantic>=2.0,<3
uvicorn[standard]>=0.24.0

# LLM Integration
openai>=1.0.0  # For GPT-4o-mini/GPT-4o

# Optional: Alternative LLM providers
# anthropic>=0.7.0  # For Claude
# google-generativeai>=0.3.0  # For Gemini

# Vector & Search
numpy>=1.24.0
scikit-learn>=1.3.0
rank-bm25>=0.2.2
chromadb>=0.4.15
sentence-transformers>=2.2.0

# FAISS (optional, for faster vector search)
faiss-cpu==1.8.0

# HTTP & Async
httpx>=0.25.0
aiohttp>=3.9.0

# Config & Environment
python-dotenv>=1.0.0
pyyaml>=6.0

# Browser Automation (for Week 2)
playwright>=1.40.0

# Database (for persistence - choose one)
# sqlalchemy>=2.0.0  # SQL databases
# redis>=5.0.0  # Redis for session storage
# psycopg2-binary>=2.9.0  # PostgreSQL driver

# Monitoring & Logging (optional but recommended)
# structlog>=23.2.0
# sentry-sdk[fastapi]>=1.38.0

# Testing (for Week 4)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx  # Already listed above, for testing API